#include "TestScurt.h"
#include "TestExtins.h"

// 25. TAD Coada cu priorități – reprezentare sub forma unei LSI cu perechi (element, prioritate)
// ordonate în funcție de priorități (în raport cu o relație de ordine R),
// cu reprezentarea înlănțuirilor pe tablou dinamic.

int main() {
    testAll();
    testAllExtins();
    return 0;
}
